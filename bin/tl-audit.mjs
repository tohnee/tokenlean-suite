#!/usr/bin/env node
/**
 * tl-audit.mjs — Analyze token usage from session transcripts.
 *
 * Usage: tl-audit [--claudecode] [--savings] [-j]
 *
 * Stub: full implementation uses 01-workflow/.claude/lib/hit-rate.mjs.
 * Run `cd 01-workflow && node .claude/lib/hit-rate.mjs` for the raw analysis.
 */
console.log(`[tokenlean] tl-audit — token usage analysis

To run a full audit:
  cd tokenlean-suite/01-workflow
  node .claude/lib/hit-rate.mjs

This will analyze Claude Code session transcripts in ~/.claude/projects/.
`);
